from setuptools import setup, find_packages

setup(
    name="auto-investment",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # requirements.txt의 내용이 이미 설치되어 있으므로 여기서는 생략
    ],
) 